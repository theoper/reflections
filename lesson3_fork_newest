# 从原始库中远程克隆到本地，命名为 upstream
git remote add upstream https://github.com/udacity/creat-your-own-adventure.git

# 转到 master 分支
git checkout master

# 从原始库中获取更新，保持本地 master 和原始库同步
git pull upstream master

# 转到 stop-drop-roll
git checkout stop-drop-roll

# 在 stop-drop-roll 分支中和 master 合并
git merge master stop-drop-roll

# 产生冲突后，修改文件

# 添加修改后的文件到暂存区
# git add fire.md

# 提交 commit
# git commit

# 将分支 stop-drop-roll 提交到 fork 后的库
git push origin stop-drop-roll
 
# 转到 master 的分支上
git checkout master

# 推送到 fork 库的 master 分支
git push origin master



